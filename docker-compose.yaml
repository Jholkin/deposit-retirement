version: '3.1'

services:
  
  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"
    
  api:
    build: .
    restart: always
    links:
      - mongo
    depends_on:
      - mongo
    volumes:
      - .:/usr/src/app
    ports:
      - "3000:3000"
